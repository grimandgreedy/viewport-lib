//! Named standard camera orientations (front, top, isometric, etc.).
//!
//! Relocated to `viewport-lib-types`; re-exported here so the renderer's
//! `crate::camera::view_preset::*` paths keep resolving.

pub use viewport_lib_types::camera::view_preset::*;
