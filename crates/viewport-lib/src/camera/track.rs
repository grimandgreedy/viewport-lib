//! Keyframe camera animation with Catmull-Rom interpolation.
//!
//! Relocated to `viewport-lib-types`; re-exported here so the renderer's
//! `crate::camera::track::*` paths keep resolving.

pub use viewport_lib_types::camera::track::*;
