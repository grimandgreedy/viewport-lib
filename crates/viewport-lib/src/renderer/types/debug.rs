//! Debug visualisation configuration.
//!
//! These settings live in `viewport-lib-types`; re-exported here so the
//! renderer's `crate::renderer::types::debug::*` paths keep resolving.

pub use viewport_lib_types::effects::debug::*;
